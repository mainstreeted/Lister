# Windows macro applier dependencies. Install with:  pip install -r requirements.txt
# These run on a native Windows Python, separate from the WSL `lister` package.
pyautogui>=0.9.54
pygetwindow>=0.0.9
pillow>=10.0

# Only needed if config.json sets "locator": "api" (the default is the free
# claude-cli backend). Uncomment to use the Anthropic API for element location.
# anthropic>=0.40.0
